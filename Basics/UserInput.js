// first install prompt-sync using
// npm install prompt-sync

const prompt = require('prompt-sync')();

var name =  prompt("Enter name:");
var age = prompt("Enter your age:Umair");

console.log("Hi! ",name, "your age is ",age)